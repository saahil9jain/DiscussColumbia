<h1> Posts </h1>

<% @posts.each do |post| %>
  <h3><%= post.question %></h3>
  <p><%= post.description %></p>
  <p><%= post.tag %></p>
  <h5><%= link_to "Edit Posting", edit_post_path(post)%></h5>

<% end %>